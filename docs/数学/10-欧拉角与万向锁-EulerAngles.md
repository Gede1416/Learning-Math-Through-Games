# 10-欧拉角与万向锁（Euler Angles & Gimbal Lock）

> 教材：《3D Math Primer for Graphics and Game Development》(2nd) Ch 8–9；《Essential Mathematics for Games》(3rd) Ch 5
> 阶段三 Day 1｜准备步骤：补齐 X 轴旋转数学工具｜开始：2026-09-03

---

## 本课流程

1. 你先完成本课新增的数学工具。
2. 工具测试全部通过后，导师再给出不超过 30 行的错误游戏使用代码。
3. 导师提出苏格拉底问题，你把回答写入 `00-我的回答.md`。
4. 你根据推导亲自修整错误使用代码。

当前只进行第 1 步，不提前修改欧拉角组合代码。

---

## 当前数学工具：绕 X 轴旋转

项目统一约定：`+X` 右、`+Y` 上、`+Z` 前，使用列向量 `v'=M·v`。

```text
Rx(θ) = |1   0      0    0|
        |0  cosθ  -sinθ  0|
        |0  sinθ   cosθ  0|
        |0   0      0    1|
```

角度参数使用“度”，而 `MathF.Sin/MathF.Cos` 接收“弧度”：

```text
radians = degrees × π / 180
```

请完成统一矩阵类型中的 `CreateRotationXDegrees`：

```text
Homework/数学/07-平移与齐次坐标/Matrix4x4.cs
```

方法当前故意返回 `Identity` 占位。只修改该方法体，不修改测试期望，也不要复制出第二套矩阵类型。

## 当前验收

`Program.cs` 现在只运行本步骤的 4 项工具测试：

1. `0°`：`+Y → +Y`
2. `+90°`：`+Y → +Z`
3. `+90°`：`+Z → -Y`
4. `-45°`：`+Z → (0, √0.5, √0.5)`，防止只对 `90°` 硬编码

初始基线应为 **1/4 PASS**。完成目标为 **4/4 PASS**。

工具通过后告诉我“写好了”。届时再进入错误 FPS 相机代码、问题回答和代码修整阶段。

---

`[数学/三维旋转-X轴旋转工具：等待实现]`
