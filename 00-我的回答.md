# 我的回答记录（游戏程序员的数学课）

> 每次苏格拉底问答的答案、纠错过程写在这里；每轮作业答案也追加。
> 对应笔记：`docs/数学/` 下按编号查看。

---

## 第一轮：向量定义与基本运算（2026-08-25 开课）

**苏格拉底问题**：斜向移动代码在什么具体游戏输入或帧率变化下会出错？数学上哪里不合理？

我的回答：

```
（第一次尝试）
1. input 的值在 -√2 和 +√2 之间,导致玩家在不同方向的最大速度不一样
2. 改成使用该向量在该点的 sin/cos 值的平方和
→ 纠错:sin/cos 方案绕路(需先求角度再重建向量),应直接让手头向量变单位向量

（第二次尝试,引导后修正）
1. 改为使用单位向量的模长
→ 再纠错:单位向量的模长就是 1,没有可"使用"的余地;正确操作是除以自身的模长
→ 标准解:dir = dir / |dir|(归一化 v̂ = v/|v|),斜向移动速度从 5√2 ≈ 7.07 m/s 回到 5 m/s
```

**作业**：`Homework/数学/第一轮-向量基本运算/Vector3.cs`

我的实现与测试结果：

```
(第一次运行:4 PASS / 3 FAIL)
[FAIL] Magnitude(3,4,0) == 5：期望 5，实际 25        ← Sqrt 结果被丢弃,返回了平方模长
[PASS] SqrMagnitude(3,4,0) == 25
[FAIL] Normalized(3,4,0) 模长为 1：期望 1，实际 0.04  ← 除以了平方模长 25 而非模长 5
[FAIL] Normalized(3,4,0).X == 0.6：期望 0.6，实际 0.12
[PASS] 加法 / 减法 / 标量乘法

(修正后:7/7 全部 PASS)
[PASS] Magnitude == 5  [PASS] SqrMagnitude == 25
[PASS] Normalized 模长 == 1  [PASS] Normalized.X == 0.6
[PASS] (3,4,0)+(1,0,0).X == 4  [PASS] (3,4,0)-(1,0,0).X == 2  [PASS] (3,4,0)*2.X == 6
```