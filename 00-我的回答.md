# 我的回答记录（游戏程序员的数学课）

> 每次苏格拉底问答的答案、纠错过程写在这里；每轮作业答案也追加。
> 对应笔记：`docs/数学/` 下按编号查看。

---

## 第一轮：向量定义与基本运算（2026-08-25 开课）

**苏格拉底问题**：斜向移动代码在什么具体游戏输入或帧率变化下会出错？数学上哪里不合理？

我的回答：

```
1. input 的值在 -根号2 和 +根号2 之间
导致玩家在不同方的最大速度不一样
改成 使用 该向量在该点的 sin cos值的平方和

（第二次修改）
1. 改为使用单位向量的模长
```

**作业**：`Homework/数学/第一轮-向量基本运算/Vector3.cs`

我的实现与测试结果：

```
7/7 全部 PASS
[PASS] Magnitude == 5  [PASS] SqrMagnitude == 25
[PASS] Normalized 模长 == 1  [PASS] Normalized.X == 0.6
[PASS] (3,4,0)+(1,0,0).X == 4  [PASS] (3,4,0)-(1,0,0).X == 2  [PASS] (3,4,0)*2.X == 6
```

---

## 第二轮：点积（2026-08-27 开课）

**苏格拉底问题**：警戒判定代码在什么具体游戏状态下会误报警戒？数学上哪里不合理？

我的回答：

```
1. toPlayer 的方向反了 应该是 T->P transform.position - player.position
现在是在玩家背面会触发报警

2. float dot = Vector3.Dot(facing, toPlayer); 会导致计算的夹角值 产生问题
0-1 会过早触发 1-~ 过晚触发
```

**作业**：`Homework/数学/第二轮-点积/DotProduct.cs`

我的实现与测试结果：

```
17/17 全部 PASS（含第一轮回归）
[PASS] Dot 垂直==0 / 平行==1 / (3,4,0)·(1,0,0)==3
[PASS] ProjectScalar(3,4,0→(1,0,0))==3  (1,0,0)→(3,4,0)==0.6
[PASS] Angle((1,0,0),(0,1,0))==90  Angle((1,0,0),(-1,0,0))==180
[PASS] IsInCone 正面0°距离1米→true  背面180°→false
[PASS] IsInCone 80°距离5米→false  80°距离0.5米→false（距离无关）
```

## 第三轮：叉积（2026-08-27 开课）

**苏格拉底问题**：左右转向判定代码在什么具体游戏状态下会朝错误方向转向？数学上哪里不合理？

我的回答：

```
1. 等于0 的时候没有判断
2. 现在是 toPlayer 指向 forward,叉积的计算顺序是从右到左
```

**作业**：`Homework/数学/第三轮-叉积/CrossProduct.cs`

我的实现与测试结果：

```
24/24 全部 PASS（含前两轮回归）
[PASS] x×y==z  y×x==-z（交换反号）  x×x==0
[PASS] |(3,0,0)×(0,4,0)|==12（面积）  cross·a==0（垂直）
[PASS] TriangleNormal(0,0,0)(1,0,0)(0,1,0)==+z
[PASS] Side：左→+1  右→-1  正前方→0
```

## 矩阵向量乘法

1. 计算顺序错误 导致旋转方向错误 应该是

```cs
   return new Vector2(
   local.X _ m00 + local.Y _ m01,
   local.X _ m10 + local.Y _ m11);
```

2. 原期望 0，1 实际 0，-1

## 综合

1. 角度计算没有使用单位向量进行计算
2.

## 线性变换

1. 期望 1，2 实际 0，2
2. 期望 1，0 实际 1，0.5
3. 实际应该

```cs
   return new Vector2(
   vertex.X + k * vertex.Y,
   vertex.Y);
```

## 平移齐次坐标

1. TransformPoint 后结果 (10, 2, 6) 正确
2. TransformDirection 值(10, 2, 6) 应该是(0,0,1) 是吧方向向量看作为 位置向量 进行计算点的平移了
3. 我没有明白你的意思
   我的理解 w是为了能够让矩阵表现出平移的特性根据
   矩阵计算展开式

   ```cs
   M00 * v.X + M01 * v.Y + M02 * v.Z + M03 * w,
   M10 * v.X + M11 * v.Y + M12 * v.Z + M13 * w,
   M20 * v.X + M21 * v.Y + M22 * v.Z + M23 * w
   ```

   w==1可以保证矩阵最右一列是 向量的平移值

4. 在 Transform 中给方向向量加入了 平移导致数据导致方向错误
